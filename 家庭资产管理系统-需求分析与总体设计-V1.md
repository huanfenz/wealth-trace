# 1. 系统定位

本系统定位为：

> **轻量级家庭资产管理与记账系统**

系统用于管理一个家庭中不同成员的：

- 支付宝、微信、银行卡等账户；
    
- 现金、活期、定期存款；
    
- 基金、债券、保险等金融资产；
    
- 信用卡欠款、借款等负债；
    
- 收入、支出、转账；
    
- 成员个人资产；
    
- 家庭整体资产；
    
- 总资产、总负债、净资产；
    
- 月度和年度收支情况。
    

第一版重点解决四个问题：

1. **家庭的钱在哪里？**
    
2. **这些钱属于哪个家庭成员？**
    
3. **这些钱是什么类型的资产？**
    
4. **这些钱为什么发生变化？**
    

核心概念：

```text
Household
解决“属于哪个家庭”

Member
解决“属于哪个成员”

Account
解决“钱在哪里”

Asset
解决“钱是什么”

Transaction
解决“钱为什么变化”
```

---

# 2. V1 设计原则

第一版设计目标：

> **简单、清晰、轻量、易开发，同时保留后续扩展能力。**

V1 暂时不实现：

```text
复杂权限系统

私人账单权限

成员级 ACL

TransactionEntry

TransactionCategory 独立表

复式记账

基金份额 / 净值模型

股票实时行情

自动银行卡同步

AI 自动记账
```

家庭 V1 默认采用：

> **同一家庭成员可以查看该家庭中的全部资产和账单。**

以后如果产生明确的隐私需求，再增加：

```text
visibility
permission
```

等权限模型。

---

# 3. 核心业务模型

整体模型：

```text
Household
   │
   ├── HouseholdMember
   │
   └── Account
          │
          ▼
        Asset
          ▲
          │
      Transaction
```

不同资产通过扩展表保存专有属性：

```text
Asset
├── TermDepositDetail
├── FundDetail
├── BondDetail
└── InsuranceDetail
```

---

# 4. Household——家庭

Household 表示一个独立家庭账本。

例如：

```text
我的家庭
```

一个家庭可以有多个成员：

```text
我的家庭
├── 王鹏
├── 配偶
└── 其他成员
```

---

# 5. household 表

```text
household
```

建议字段：

|字段|类型|说明|
|---|---|---|
|id|BIGINT|主键|
|name|VARCHAR(100)|家庭名称|
|created_at|DATETIME|创建时间|
|updated_at|DATETIME|修改时间|

例如：

```text
id = 1

name = 我的家庭
```

---

# 6. HouseholdMember——家庭成员

建立：

```text
household_member
```

用于表示家庭中的成员。

建议字段：

|字段|类型|说明|
|---|---|---|
|id|BIGINT|主键|
|household_id|BIGINT|所属家庭|
|name|VARCHAR(100)|成员名称|
|role|VARCHAR(32)|角色|
|status|VARCHAR(32)|状态|
|created_at|DATETIME|创建时间|
|updated_at|DATETIME|修改时间|

角色第一版可以简单定义：

```text
OWNER
MEMBER
```

状态：

```text
ACTIVE
INACTIVE
```

---

# 7. Account——账户

Account 表示：

> **钱在哪里。**

例如：

```text
工商银行

招商银行

支付宝

微信

证券账户

现金钱包
```

账户必须属于：

```text
某个家庭
+
某个家庭成员
```

例如：

```text
王鹏
└── 工商银行工资卡
```

或者：

```text
配偶
└── 支付宝
```

---

# 8. Account 属主设计

账户直接保存：

```text
household_id

owner_member_id
```

这样可以直接查询：

```sql
SELECT *
FROM account
WHERE household_id = ?
  AND owner_member_id = ?;
```

---

# 9. 账户类型

建议支持：

```text
BANK

ALIPAY

WECHAT

CASH

SECURITIES

INSURANCE

OTHER
```

含义：

```text
BANK
银行账户

ALIPAY
支付宝

WECHAT
微信

CASH
现金

SECURITIES
证券账户

INSURANCE
保险机构账户

OTHER
其他
```

这些属于核心枚举。

---

# 10. account 表

```text
account
```

字段建议：

|字段|类型|说明|
|---|---|---|
|id|BIGINT|主键|
|household_id|BIGINT|所属家庭|
|owner_member_id|BIGINT|属主成员|
|name|VARCHAR(100)|账户名称|
|type|VARCHAR(32)|账户类型|
|institution_name|VARCHAR(100)|银行或机构|
|account_no_masked|VARCHAR(64)|脱敏账号|
|remark|VARCHAR(500)|备注|
|enabled|BOOLEAN|是否启用|
|created_at|DATETIME|创建时间|
|updated_at|DATETIME|修改时间|

---

# 11. Account 与 Asset

一个账户下面可以有多个资产。

关系：

```text
Account 1 ---- N Asset
```

例如：

```text
王鹏
└── 工商银行
    ├── 活期            20,000
    ├── 一年定期        50,000
    └── 三年定期       100,000
```

工商银行账户总资产：

```text
170,000
```

---

# 12. 账户余额设计

Account 本身不保存余额。

账户余额由其下面所有有效 Asset 汇总：

```sql
SELECT SUM(current_balance)
FROM asset
WHERE account_id = ?
  AND status = 'ACTIVE';
```

这样不会同时维护：

```text
account.balance
```

和：

```text
asset.current_balance
```

两套余额。

---

# 13. Asset——资产

Asset 表示：

> **这笔钱是什么。**

例如：

```text
现金

活期

定期存款

基金

债券

保险

负债
```

每个 Asset 必须属于一个 Account。

---

# 14. Asset 属主设计

从业务逻辑上：

```text
Asset 属主
=
Account 属主
```

但为了减少大量联表查询，Asset 中冗余保存：

```text
household_id

owner_member_id
```

因此可以直接查询某成员资产：

```sql
SELECT *
FROM asset
WHERE household_id = ?
  AND owner_member_id = ?;
```

而无需每次：

```text
Asset
→ Account
→ HouseholdMember
```

进行联表。

约定：

> Account.owner_member_id 是逻辑来源。

创建 Asset 时：

```text
asset.owner_member_id
=
account.owner_member_id
```

---

# 15. 资产类型

V1 支持：

```text
CASH

TERM_DEPOSIT

FUND

BOND

BOND_FUND

INSURANCE

LIABILITY

OTHER
```

含义：

```text
CASH
现金 / 活期

TERM_DEPOSIT
定期存款

FUND
基金

BOND
债券

BOND_FUND
债券基金（持有期 / 滚动持有，见 §23 对齐说明）

INSURANCE
保险

LIABILITY
负债

OTHER
其他
```

以后可以扩展：

```text
STOCK

GOLD

REAL_ESTATE

CRYPTO
```

---

# 16. asset 主表

```text
asset
```

Asset 主表只保存真正通用的属性。

|字段|类型|说明|
|---|---|---|
|id|BIGINT|主键|
|household_id|BIGINT|所属家庭|
|owner_member_id|BIGINT|属主|
|account_id|BIGINT|所属账户|
|name|VARCHAR(100)|资产名称|
|asset_type|VARCHAR(32)|资产类型|
|opening_balance|DECIMAL(20,4)|初始金额|
|current_balance|DECIMAL(20,4)|当前价值|
|status|VARCHAR(32)|状态|
|remark|VARCHAR(500)|备注|
|created_at|DATETIME|创建时间|
|updated_at|DATETIME|修改时间|

---

# 17. Asset 不保存哪些字段

主表不保存：

```text
locked_until

maturity_date

interest_rate

fund_code

policy_no
```

这些字段只属于部分资产类型。

因此放到对应 Detail 表。

原则：

> **主表负责共性，Detail 表负责个性。**

---

# 18. current_balance 定义

统一定义：

> **当前计入家庭净资产统计的价值。**

现金 / 活期：

```text
current_balance
=
当前余额
```

定期：

```text
current_balance
=
当前计入资产的金额
```

基金：

```text
current_balance
=
基金当前总价值
```

债券：

```text
current_balance
=
债券当前总价值
```

保险：

```text
current_balance
=
当前计入资产统计的价值
```

负债：

```text
current_balance
=
负数
```

---

# 19. 负债设计

负债作为特殊 Asset：

```text
asset_type = LIABILITY
```

约定：

```text
普通资产
current_balance >= 0

负债
current_balance <= 0
```

例如：

```text
现金                +50,000

定期存款           +100,000

基金                +30,000

信用卡欠款           -8,000

个人借款            -20,000
```

净资产：

```text
152,000
```

---

# 20. 定期存款设计

建立：

```text
term_deposit_detail
```

字段：

|字段|类型|说明|
|---|---|---|
|asset_id|BIGINT|Asset ID|
|annual_interest_rate|DECIMAL(10,6)|年利率|
|start_date|DATE|起息日期|
|maturity_date|DATE|到期日期|
|term_value|INT|存款期限|
|term_unit|VARCHAR(16)|DAY/MONTH/YEAR|
|interest_type|VARCHAR(32)|计息方式|
|auto_rollover|BOOLEAN|自动转存|
|maturity_action|VARCHAR(32)|到期处理方式|

例如：

```text
annual_interest_rate = 0.018500

start_date = 2026-09-19

term_value = 3

term_unit = YEAR

maturity_date = 2029-09-19
```

> **对齐说明（每日维护）**：`start_date` / `term_value` / `term_unit` 必填，`maturity_date`
> 缺省时由服务端按自然月 / 自然年计算（目标月无对应日取月末），允许用户手工修正。
> 开启自动续存（`auto_rollover=true`）时，每日维护在到期当天（业务日期 `today >= maturity_date`）
> 推进到下一存期并同步 `start_date`；不自动修改本金、当前金额与利率，到期状态由日期动态推导。
> 详见 [`docs/设计问题说明.md`](./docs/设计问题说明.md) 第 17 节。
>
> 本金不再单独保存在明细表，统一取 `asset.opening_balance`（见第 18 节）。

---

# 21. 定期存款 V1 价值

第一版：

```text
asset.current_balance
=
本金
```

或者银行当前显示金额。

暂时不做：

```text
每日应计利息

动态计算预计本息
```

---

# 22. 基金设计

V1 基金不管理：

```text
基金份额

基金净值

成本价

每份价格
```

只管理：

> **这只基金当前一共值多少钱。**

当前价值直接放：

```text
asset.current_balance
```

建立：

```text
fund_detail
```

---

# 23. fund_detail

字段建议：

|字段|类型|说明|
|---|---|---|
|asset_id|BIGINT|Asset ID|
|fund_code|VARCHAR(32)|基金代码|
|fund_type|VARCHAR(32)|基金类型|
|lock_start_date|DATE|锁定开始，可空|
|lock_end_date|DATE|锁定结束，可空|

例如：

```text
Asset

name = 某债券基金
asset_type = FUND
current_balance = 50000
```

对应：

```text
FundDetail

fund_code = 000001
fund_type = BOND
lock_end_date = 2027-03-01
```

> **对齐说明（BOND_FUND）**：普通基金用 `asset_type=FUND` + `fund_detail`。
> 债券基金（持有期 / 滚动持有）统一用 `asset_type=BOND_FUND` + `bond_fund_detail`，
> 通过 `holding_mode`（`MIN_HOLDING` / `ROLLING`）区分，并以 `first_redeem_date` /
> `next_redeem_date` 表达赎回时间；字段与每日维护规则见
> [`docs/设计问题说明.md`](./docs/设计问题说明.md) 第 14–16 节。
>
> **去重说明**：明细表不再保存 `fund_name` / `bond_name` / `principal`，名称一律使用
> `asset.name`，本金一律使用 `asset.opening_balance`（见第 18 节）。

---

# 24. 债券设计

建立：

```text
bond_detail
```

字段：

|字段|类型|说明|
|---|---|---|
|asset_id|BIGINT|Asset ID|
|bond_code|VARCHAR(32)|债券代码|
|annual_coupon_rate|DECIMAL(10,6)|年利率|
|purchase_date|DATE|购买日期|
|maturity_date|DATE|到期日期|
|lock_end_date|DATE|锁定结束，可空|

债券当前价值：

```text
asset.current_balance
```

不再重复保存：

```text
current_value
```

---

# 25. 保险设计

建立：

```text
insurance_detail
```

字段：

|字段|类型|说明|
|---|---|---|
|asset_id|BIGINT|Asset ID|
|policy_no|VARCHAR(64)|保单号|
|insurance_company|VARCHAR(100)|保险公司|
|product_name|VARCHAR(100)|产品名称|
|insurance_type|VARCHAR(32)|类型|
|effective_date|DATE|生效日期|
|maturity_date|DATE|到期日期|
|annual_premium|DECIMAL(20,4)|年保费|
|total_paid_premium|DECIMAL(20,4)|累计保费|
|insured_amount|DECIMAL(20,4)|保额|
|payment_years|INT|缴费年限|

当前计入资产的价值统一使用：

```text
asset.current_balance
```

不重复保存：

```text
cash_value
```

---

# 26. Detail 表原则

统一原则：

```text
Asset
保存所有资产共性字段
```

```text
TermDepositDetail
保存定期专有属性
```

```text
FundDetail
保存基金专有属性
```

```text
BondDetail
保存债券专有属性
```

```text
InsuranceDetail
保存保险专有属性
```

金额当前值统一只维护：

```text
asset.current_balance
```

---

# 27. Transaction——交易

Transaction 表示：

> **某个家庭成员的某项资产发生了一次金额变化。**

V1 中：

> 一条 Transaction 永远只直接影响一个 Asset。

这是整个交易模型的重要原则。

---

# 28. Transaction 属主

Transaction 中直接冗余保存：

```text
household_id

owner_member_id
```

这样可以直接查询：

```sql
SELECT *
FROM transaction
WHERE household_id = ?
  AND owner_member_id = ?;
```

而不需要通过：

```text
Transaction
→ Asset
→ Account
```

再判断属主。

---

# 29. 交易类型

V1 交易类型：

```text
INCOME

EXPENSE

TRANSFER_IN

TRANSFER_OUT

ADJUSTMENT
```

含义：

```text
INCOME
收入

EXPENSE
支出

TRANSFER_IN
转入

TRANSFER_OUT
转出

ADJUSTMENT
余额调整
```

---

# 30. 为什么拆分 TRANSFER_IN / TRANSFER_OUT

系统统一采用：

> **一次转账产生两条 Transaction。**

例如：

```text
工商银行
→
支付宝

5000
```

产生：

```text
Transaction A

type = TRANSFER_OUT

asset = 工商银行

amount = 5000
```

以及：

```text
Transaction B

type = TRANSFER_IN

asset = 支付宝

amount = 5000
```

两条记录通过：

```text
transfer_group_id
```

关联。

---

# 31. 同成员转账

同一个成员自己的资产之间转账，也产生两条记录。

例如：

```text
王鹏工商银行
→
王鹏支付宝

5000
```

记录：

```text
Transaction A

owner = 王鹏
asset = 工商银行
type = TRANSFER_OUT
amount = 5000
```

以及：

```text
Transaction B

owner = 王鹏
asset = 支付宝
type = TRANSFER_IN
amount = 5000
```

两条记录：

```text
transfer_group_id = 10001
```

---

# 32. 跨成员转账

例如：

```text
王鹏工商银行
→
配偶支付宝

5000
```

王鹏：

```text
Transaction A

owner_member_id = 王鹏

asset_id = 王鹏工商银行

type = TRANSFER_OUT

amount = 5000

transfer_group_id = 10002
```

配偶：

```text
Transaction B

owner_member_id = 配偶

asset_id = 配偶支付宝

type = TRANSFER_IN

amount = 5000

transfer_group_id = 10002
```

这样每条 Transaction：

```text
只属于一个成员

只影响一个资产
```

---

# 33. 这种转账模型的优势

所有交易模型完全统一：

```text
收入
某资产 +10000

支出
某资产 -35

转出
某资产 -5000

转入
某资产 +5000

调整
某资产 ±1000
```

因此 Transaction 不再需要：

```text
target_asset_id
```

数据库结构更简单。

---

# 34. transaction 表

```text
transaction
```

建议字段：

|字段|类型|说明|
|---|---|---|
|id|BIGINT|主键|
|household_id|BIGINT|所属家庭|
|owner_member_id|BIGINT|属主成员|
|asset_id|BIGINT|发生变化的资产|
|type|VARCHAR(32)|交易类型|
|category|VARCHAR(64)|收入/支出分类|
|amount|DECIMAL(20,4)|金额|
|transfer_group_id|BIGINT|转账关联 ID，可空|
|balance_before|DECIMAL(20,4)|变化前余额，可空|
|balance_after|DECIMAL(20,4)|变化后余额，可空|
|transaction_time|DATETIME|交易时间|
|remark|VARCHAR(500)|备注|
|status|VARCHAR(32)|状态|
|created_at|DATETIME|创建时间|
|updated_at|DATETIME|修改时间|

---

# 35. category 设计

V1 不建立：

```text
transaction_category
```

独立表。

直接：

```text
transaction.category
```

保存字符串。

例如：

```text
餐饮

交通

购物

工资

奖金

红包
```

默认分类放配置文件。

---

# 36. 默认支出分类

例如：

```text
餐饮

交通

购物

娱乐

居住

医疗

教育

通讯

人情

其他
```

---

# 37. 默认收入分类

例如：

```text
工资

奖金

红包

利息收入

投资收益

退款

其他
```

---

# 38. 收入示例

工资：

```text
10000
```

Transaction：

```text
owner_member_id = 王鹏

asset_id = 工商银行活期

type = INCOME

category = 工资

amount = 10000
```

资产：

```text
+10000
```

---

# 39. 支出示例

午饭：

```text
35
```

Transaction：

```text
owner_member_id = 王鹏

asset_id = 支付宝余额

type = EXPENSE

category = 餐饮

amount = 35
```

资产：

```text
-35
```

---

# 40. 信用卡消费

信用卡当前：

```text
0
```

刷卡：

```text
100
```

Transaction：

```text
asset_id = 信用卡负债

type = EXPENSE

category = 餐饮

amount = 100
```

负债：

```text
0
→
-100
```

---

# 41. 信用卡还款

假设：

```text
银行卡
10000

信用卡
-5000
```

还款：

```text
3000
```

产生两个 Transaction。

银行卡：

```text
type = TRANSFER_OUT

amount = 3000
```

信用卡：

```text
type = TRANSFER_IN

amount = 3000
```

结果：

```text
银行卡
10000
→
7000
```

信用卡：

```text
-5000
→
-2000
```

---

# 42. 活期转定期

例如：

```text
工商银行活期
→
工商银行三年定期

100000
```

产生：

```text
TRANSFER_OUT

活期
-100000
```

和：

```text
TRANSFER_IN

三年定期
+100000
```

两条 Transaction 共用：

```text
transfer_group_id
```

总资产不变。

---

# 43. ADJUSTMENT——余额调整

现实中可能存在：

```text
漏记账

历史误差

基金价值变化

债券价值变化

手工校准
```

统一使用：

```text
ADJUSTMENT
```

例如基金：

```text
原值
50000

当前值
51000
```

Transaction：

```text
type = ADJUSTMENT

asset_id = 某基金

amount = 1000
```

资产：

```text
50000
→
51000
```

---

# 44. Transaction 金额方向

数据库中的：

```text
amount
```

建议统一保存：

> **正数的绝对金额。**

例如：

```text
EXPENSE
amount = 35

TRANSFER_OUT
amount = 5000
```

而不是：

```text
-35

-5000
```

具体加减由 `type` 决定。

规则：

```text
INCOME
+amount

EXPENSE
-amount

TRANSFER_IN
+amount

TRANSFER_OUT
-amount
```

ADJUSTMENT 可以特殊处理。

建议增加：

```text
adjustment_direction
```

也可以允许 ADJUSTMENT 的 `amount` 为正负数。

为了保持 V1 简单：

```text
ADJUSTMENT
amount 可正可负
```

其他类型：

```text
amount > 0
```

---

# 45. 资产变化追溯

因为每条 Transaction 只对应一个 Asset，所以查询非常简单：

```sql
SELECT *
FROM transaction
WHERE asset_id = ?
ORDER BY transaction_time;
```

即可看到：

```text
收入

支出

转入

转出

余额调整
```

---

# 46. 成员账单查询

```sql
SELECT *
FROM transaction
WHERE household_id = ?
  AND owner_member_id = ?
ORDER BY transaction_time DESC;
```

可以直接查询某个家庭成员的全部流水。

无需联表。

---

# 47. 家庭账单查询

```sql
SELECT *
FROM transaction
WHERE household_id = ?
ORDER BY transaction_time DESC;
```

即可得到整个家庭的流水。

---

# 48. 余额重建

规定：

> **任何 Asset.current_balance 变化都必须产生 Transaction。**

理论上：

```text
current_balance
=
opening_balance
+
所有 Transaction 的资产变化
```

规则：

```text
INCOME
+amount

EXPENSE
-amount

TRANSFER_IN
+amount

TRANSFER_OUT
-amount

ADJUSTMENT
+amount
```

其中 ADJUSTMENT 的 amount 可正可负。

---

# 49. balance_before / balance_after

推荐保留：

```text
balance_before

balance_after
```

例如：

```text
午饭 35 元

balance_before = 2000

balance_after = 1965
```

主要用于：

```text
问题排查

审计

前端展示
```

属于冗余辅助字段。

---

# 50. 数据一致性

创建交易和更新 Asset 必须放在一个数据库事务中。

例如：

```text
BEGIN

INSERT transaction

UPDATE asset.current_balance

COMMIT
```

如果失败：

```text
ROLLBACK
```

转账则：

```text
BEGIN

INSERT TRANSFER_OUT

UPDATE source asset

INSERT TRANSFER_IN

UPDATE target asset

COMMIT
```

必须保证四步全部成功或全部失败。

---

# 51. 总资产计算

家庭总资产：

```sql
SELECT SUM(current_balance)
FROM asset
WHERE household_id = ?
  AND asset_type <> 'LIABILITY'
  AND status = 'ACTIVE';
```

---

# 52. 成员总资产

```sql
SELECT SUM(current_balance)
FROM asset
WHERE household_id = ?
  AND owner_member_id = ?
  AND asset_type <> 'LIABILITY'
  AND status = 'ACTIVE';
```

---

# 53. 总负债

```sql
SELECT ABS(SUM(current_balance))
FROM asset
WHERE household_id = ?
  AND asset_type = 'LIABILITY'
  AND status = 'ACTIVE';
```

---

# 54. 净资产

家庭净资产：

```text
家庭净资产
=
SUM(asset.current_balance)
```

也就是：

```text
总资产 - 总负债
```

例如：

```text
总资产      ￥1,000,000

总负债        ￥300,000

净资产        ￥700,000
```

---

# 55. 成员净资产

同样可以计算：

```text
成员净资产
=
SUM(
  asset.current_balance
  WHERE owner_member_id = ?
)
```

例如：

```text
王鹏净资产
￥350,000

配偶净资产
￥280,000
```

---

# 56. 金额字段设计

> V1 仅支持人民币（CNY），所有表不保存币种字段，统计直接求和，不做汇率换算。

MySQL：

```sql
DECIMAL(20,4)
```

Java：

```java
BigDecimal
```

禁止：

```text
float

double
```

---

# 57. 利率字段

推荐：

```text
DECIMAL(10,6)
```

例如：

```text
1.85%
```

存为：

```text
0.018500
```

---

# 58. V1 核心数据库表

V1 共保留：

```text
household

household_member

account

asset

term_deposit_detail

fund_detail

bond_detail

insurance_detail

transaction
```

共：

> **9 张核心业务表**

不包含：

```text
transaction_entry

transaction_category

permission
```

---

# 59. 数据库关系

```text
household
   │
   │ 1:N
   ▼
household_member
```

```text
household
   │
   │ 1:N
   ▼
account
```

```text
household_member
   │
   │ 1:N
   ▼
account
```

```text
account
   │
   │ 1:N
   ▼
asset
```

```text
asset
   │
   ├── 1:0..1 term_deposit_detail
   ├── 1:0..1 fund_detail
   ├── 1:0..1 bond_detail
   └── 1:0..1 insurance_detail
```

```text
asset
   │
   │ 1:N
   ▼
transaction
```

---

# 60. ER 逻辑结构

```text
              ┌────────────────────┐
              │     household      │
              └─────────┬──────────┘
                        │
                ┌───────┴────────┐
                │                │
                ▼                ▼
      ┌─────────────────┐   ┌───────────────┐
      │household_member │   │    account    │
      └────────┬────────┘   └───────┬───────┘
               │                    │
               └──────────┐         │
                          ▼         ▼
                        account
                           │
                           ▼
                         asset
                           │
             ┌─────────────┼─────────────┐
             │             │             │
             ▼             ▼             ▼
     term_deposit      fund_detail   bond_detail
        _detail
                           │
                           ▼
                   insurance_detail


                         asset
                           │
                           │ 1:N
                           ▼
                     transaction
```

---

# 61. 推荐索引

Account：

```sql
INDEX idx_account_household_owner
(household_id, owner_member_id);
```

Asset：

```sql
INDEX idx_asset_household_owner
(household_id, owner_member_id);
```

```sql
INDEX idx_asset_account
(account_id);
```

Transaction：

```sql
INDEX idx_transaction_household_owner_time
(household_id, owner_member_id, transaction_time);
```

```sql
INDEX idx_transaction_asset_time
(asset_id, transaction_time);
```

```sql
INDEX idx_transaction_transfer_group
(transfer_group_id);
```

---

# 62. 首页设计

家庭首页首先展示：

```text
家庭净资产

￥700,000
```

然后：

```text
总资产      ￥1,000,000

总负债        ￥300,000

净资产        ￥700,000
```

---

# 63. 按成员统计

例如：

```text
王鹏          ￥350,000

配偶          ￥280,000

其他成员       ￥70,000
```

点击成员：

```text
王鹏

├── 工商银行      ￥180,000
├── 支付宝         ￥20,000
├── 微信            ￥5,000
└── 证券账户       ￥145,000
```

---

# 64. 按账户统计

例如：

```text
工商银行        ￥180,000

建设银行        ￥200,000

支付宝           ￥50,000

微信             ￥10,000

证券账户        ￥290,000
```

---

# 65. 按资产类型统计

例如：

```text
现金 / 活期       ￥100,000

定期存款          ￥300,000

基金              ￥200,000

债券              ￥150,000

保险              ￥250,000
```

---

# 66. 家庭收支统计

首页展示：

```text
本月收入

本月支出

本月结余
```

例如：

```text
本月收入      ￥30,000

本月支出      ￥16,000

本月结余      ￥14,000
```

同时可以按：

```text
owner_member_id
```

统计各成员收支。

---

# 67. 按分类统计

直接通过：

```text
transaction.category
```

统计。

例如：

```text
餐饮      ￥3200

交通      ￥1200

购物      ￥4800

娱乐      ￥1500
```

---

# 68. V1 核心功能

第一版实现：

```text
家庭管理
   ↓
成员管理
   ↓
账户管理
   ↓
资产管理
   ↓
收入
   ↓
支出
   ↓
转账
   ↓
余额调整
   ↓
资产变化追溯
   ↓
成员资产统计
   ↓
家庭资产统计
   ↓
总资产 / 总负债 / 净资产
   ↓
月度收支统计
```

---

# 69. V1 暂不实现

第一版暂不实现：

```text
家庭成员账单隐私

复杂权限 ACL

TransactionEntry

TransactionCategory 表

基金份额

基金净值

复式记账

预算管理

银行卡自动同步

支付宝自动同步

微信自动同步

基金实时行情

股票实时行情

多币种自动汇率

AI 自动记账
```

---

# 70. 后续扩展

后续可以增加：

```text
账单权限

预算管理

账单导入

股票

黄金

资产历史快照

净资产趋势

收益率

定期到期提醒

保险缴费提醒

负债还款提醒

AI 自动分类

AI 自动记账

AI 家庭财务分析
```

---

# 71. 最终核心架构

V1 最终核心结构：

```text
                   Household
                       │
             ┌─────────┴─────────┐
             │                   │
             ▼                   ▼
      HouseholdMember         Account
                                  │
                                  ▼
                                Asset
                                  │
                                  ▼
                             Transaction
```

Asset 通过：

```text
TermDepositDetail

FundDetail

BondDetail

InsuranceDetail
```

扩展不同资产的专有属性。

---

# 72. 最终设计原则

系统始终坚持：

> **Household 管“属于哪个家庭”。**

> **Member 管“属于哪个成员”。**

> **Account 管“钱在哪里”。**

> **Asset 管“钱是什么”。**

> **Transaction 管“钱为什么发生变化”。**

家庭归属方面：

> **Account 是逻辑上的资产属主来源，但 Asset 和 Transaction 冗余保存 owner_member_id 与 household_id，用较少的存储冗余换取更简单、高效的查询。**

交易方面：

> **一条 Transaction 只属于一个成员，只影响一个 Asset。**

转账方面：

> **一次转账统一拆成 TRANSFER_OUT + TRANSFER_IN 两条 Transaction，通过 transfer_group_id 关联。**

数据表方面：

> **Asset 只保存公共属性，Detail 表只保存专有属性。**

资产价值方面：

> **当前价值统一只保存在 asset.current_balance。**

V1 优先保证：

```text
家庭归属正确

成员归属正确

资产余额正确

交易流水完整

转账记录一致

资产变化可追溯

统计结果正确
```

在此基础上，再逐步增加家庭隐私、预算、行情、投资分析和 AI 能力。